# 📝 作業提交資訊

## 1. 前端網頁網址

### 本地開發環境
- **網址**: `http://localhost:5173`
- **啟動方式**: 
  ```bash
  cd frontend
  npm run dev
  ```

### 部署網址（Vercel）
- **網址**: `https://你的前端網址.vercel.app`（部署後填入）
- **部署指南**: 請參考 `Vercel部署指南.md`

> ⚠️ **注意**: 部署完成後，請將實際網址填入上方。部署步驟請參考 `Vercel部署指南.md`。

---

## 2. API 伺服器網址

### 本地開發環境
- **網址**: `http://localhost:8000`
- **API 文件**: `http://localhost:8000/docs` (Swagger UI)
- **啟動方式**: 
  ```bash
  cd backend
  uvicorn main:app --reload
  ```

### 部署網址（Vercel）
- **網址**: `https://你的後端網址.vercel.app`（部署後填入）
- **API 文件**: `https://你的後端網址.vercel.app/docs`
- **部署指南**: 請參考 `Vercel部署指南.md`

> ⚠️ **注意**: 部署完成後，請將實際網址填入上方。部署步驟請參考 `Vercel部署指南.md`。

---

## 3. 前端網頁程式 GitHub

- **倉庫網址**: `https://github.com/ZoeeYee/Resrv`
- **前端程式碼位置**: `frontend/` 目錄
- **主要檔案**:
  - `frontend/src/` - React 元件和頁面
  - `frontend/src/config/firebase.js` - Firebase 配置
  - `frontend/src/context/AuthContext.jsx` - Firebase 認證整合
  - `frontend/src/components/LoginRegisterModal.jsx` - 登入/註冊畫面

---

## 4. API 伺服器程式 GitHub

- **倉庫網址**: `https://github.com/ZoeeYee/Resrv`
- **後端程式碼位置**: `backend/` 目錄
- **主要檔案**:
  - `backend/main.py` - FastAPI 主程式
  - `backend/auth.py` - Firebase JWT 驗證和 Secured API
  - `backend/models.py` - 資料庫模型
  - `backend/schemas.py` - API 資料結構
  - `backend/database.py` - 資料庫連接

---

## 📋 完整提交資訊（複製用）

```
前端網頁網址: http://localhost:5173
API伺服器網址: http://localhost:8000
前端網頁程式Github: https://github.com/ZoeeYee/Resrv (frontend/ 目錄)
API伺服器程式Github: https://github.com/ZoeeYee/Resrv (backend/ 目錄)
```

---

## 🚀 部署指南

### Vercel 部署（推薦）

詳細的部署步驟請參考：**`Vercel部署指南.md`**

#### 快速步驟：
1. **部署後端**：
   - 訪問 [Vercel](https://vercel.com/)
   - 創建新專案，選擇 `ZoeeYee/Resrv` 倉庫
   - Root Directory 設定為 `backend`
   - 添加環境變數（見 `Vercel部署指南.md`）
   - 部署並記下網址

2. **部署前端**：
   - 再次創建新專案，選擇同一個倉庫
   - Root Directory 設定為 `frontend`
   - 添加環境變數，**重要**：`VITE_API_BASE` 設為後端網址
   - 部署並記下網址

3. **更新後端 CORS**：
   - 將前端網址添加到 `backend/main.py` 的 `ALLOWED_ORIGINS`
   - Commit 並 push，Vercel 會自動重新部署

詳細說明請查看 `Vercel部署指南.md` 文件。

---

## ✅ 功能確認清單

- [x] Firebase Authentication 整合（前端登入/註冊）
- [x] Firebase JWT 驗證（後端 Secured API）
- [x] 使用者註冊功能
- [x] 使用者登入功能
- [x] 使用者登出功能
- [x] 取得目前使用者資訊 API (`/auth/me`)

---

## 📞 如有問題

- GitHub Issues: https://github.com/ZoeeYee/Resrv/issues
- 查看 `啟動指南.md` 了解如何啟動本地開發環境

